{{- if .Values.userOpsIndexer.ingress.enabled -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "blockscout-user-ops-indexer.fullname" . }}
  labels:
    {{- include "blockscout-user-ops-indexer.labels" . | nindent 4 }}
  {{- with .Values.userOpsIndexer.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.userOpsIndexer.ingress.className }}
  ingressClassName: {{ .Values.userOpsIndexer.ingress.className }}
  {{- end }}
  {{- if .Values.userOpsIndexer.ingress.tls.enabled }}
  tls:
  - hosts:
    - {{ .Values.userOpsIndexer.ingress.hostname }}
    secretName: {{ include "blockscout-user-ops-indexer.fullname" . }}-tls
  {{- end }}
  rules:
  - host: {{ .Values.userOpsIndexer.ingress.hostname }}
    http:
      paths:
      {{- range .Values.userOpsIndexer.ingress.paths }}
      - path: {{ .path }}
        pathType: {{ .pathType }}
        backend:
          service:
            name: {{ include "blockscout-user-ops-indexer.fullname" $ }}
            port:
              number: {{ $.Values.userOpsIndexer.service.httpPort }}
      {{- end }}
{{- end }}

